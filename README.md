This repository contains refactored code of the 

# [Twilio Voice Swift Quickstart for iOS](https://github.com/twilio/voice-quickstart-swift)

Here are 3 branches: 
- "CallKit" contains version whish uses UIKit + MVVM + CallKit framework (old more than two years ago version)
- "SwiftUI" contains version which uses SwiftUI + MVVM (current actual version)
- "RIBs" contains version which uses SwiftUI + RIBs (future version, in progress now)

26-06-2021
